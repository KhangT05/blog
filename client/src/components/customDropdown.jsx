import {
    DropdownMenu,
    DropdownMenuTrigger
} from "./ui/dropdown-menu"

const customDropdown = ({
    trigger
}) => {
    return (
        <DropdownMenu>
            <DropdownMenuTrigger asChild>

            </DropdownMenuTrigger>
        </DropdownMenu>
    )
}
export default customDropdown